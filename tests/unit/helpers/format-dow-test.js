
import { formatDow } from 'nofrills-sleep-tracker/helpers/format-dow';
import { module, test } from 'qunit';

module('Unit | Helper | format dow');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatDow([42]);
  assert.ok(result);
});

